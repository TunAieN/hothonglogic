# Legacy Reference Material

Files in this directory describe earlier project states and are retained only for historical context. They are not current setup instructions or production runbooks.

In particular, `database_schema.sql` predates the current Laravel migration history, contains demo seed data, and must not be used to create or update a current database. The only database schema source of truth is `logistics-backend/database/migrations/`.
